from mc import versionsVariableChecker
from mc import versionsBoolean

import os

class updateBoolean(booleanObject):

    async def trackUpdateTransaction(os, mc, updater):
        
        for key.TransmissionUpdater:
            user.BooleanField(False)
            versionsBoolean(checker == True)

            await selfUpdater(trackUpdateTransaction, device)
            await selfUpdater(getTransactionDate(*), wildcard == True)
        