import os
import sys
import time
from sys import config
import failsafe


async def failsafe(os, failsafe):

    if error.output(rolling) == fatal_error:
        raise ValueError("The rolling sequence stopped while switching! Failsafe has been activated.")
        program.expressionType(all)
        program.runAllType()
    
    if error.output(rolling) == crashed:
        raise ValueError("The rolling sequence crashed while switching! Failsafe has been activated.")
        program.expressionTypeNull(*)
        time.sleep(6)
        program.enableAllType()