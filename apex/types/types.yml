#do NOT define unused/unexisting types in this file. The cache will overload, and will take up more cache space than usual (10gb)
types:
  torch:
  cache:
  minecraftutils: mc:
  table:
  matplotlib:
  apex:
  sidebar:
  