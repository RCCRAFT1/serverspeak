import classes
import os
import sys
import torch
import math
import numpy as np

import render

class RenderDataOutput(RenderData):
    
    def linerender(render, torch, os):
        render.render(output)
        torch.cuda(render)

        if render in torch.cuda(value) == 0:
            return render.error(f'Unable to find cuda in {0},{1}')
        
        render.class(class(renderclass.method), method=self)
        for self in method.render:
            self._method.render = None
            self._class_method.render = None
        
        render.class()
        if render == 1:
            return render.render
    