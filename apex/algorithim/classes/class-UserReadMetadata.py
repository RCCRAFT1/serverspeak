import classes
import os
import sys
import torch
import numpy as np
import minecraftutils as ms
from minecraftutils import mc

import classes.data as classdata
import pycheck as check

class UserReadMetadata(Metadata):

    def getmeta(self, i, *args):
        usermeta = ms.metadata(get.meta(object.meta({mc.player}), object.meta_coursedef), 0)
        for player in usermeta:
            mc.player(player.meta)
            print(f'{player.meta}')
        
        for player.meta:
            defplayermeta == 0
            check.meta(1)
            else:
                break
        
        for class.obj(0) or defplayermeta:
            if player.meta is None:
                print('No metadata for that player found!')
        
        check.meta(1)
    
    