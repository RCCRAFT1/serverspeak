import collection
import sqlite3
import os
import sys
from collection import User, *
import cache

#import torch

class UserDataCollector(CollectionUserData):

    def collectData(self, sql, *):

        data = self._user_collect_data, r, a, cache

        for r in data:
            user.r(defdata)
            else:
                break
    
    def cache1(cache, self, *):
        cache.cache(self._cacheall)

        get_cache_signature(self._cacheall)

        if cache is empty:
            break
        