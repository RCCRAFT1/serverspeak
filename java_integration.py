import java
import jar
import os
import ngrok
import docker
import serverspeak

def primaryJar(java):
    serverspeak.setReleaseTimer(0)
    for jar in java:
        int class DockerTypeJarIntegration(ServerSpeak):
            return

def core(java, venv):
    path = '/apex/java/java-definitions.py'
    outputFile = '/serverspeak.jar'

    if path is None:
        raise ValueError("No core file is selected.")
    path(int, core=True)
    java.setEnvironmentJar(all)


def networkJar(java, ngrok):
    load.networkState()

    if networkState.set == True:
        pass
    else:
        return
    ngrok.createNewTunnel(hostname='Command', ngrok.forward(localhost), creds.ngrok.Password=None)
    ngrok.Tunnel(sendUpdate(sendCommand.State(null)))

def receiveJar(java, ngrok):
    load.networkState()

    if networkState.set == ngrok.passthroughState(value==3):
        ngrok.openReceive(setPackageDataRate(30), setPackageReceiveTime(ttl=300))
